namespace Company.HardwareProgrammer;

public class HardwareUpdater
{
	public async Task Update(IEnumerable<string> deviceNames, IHardwareWriter hardwareWriter, CancellationToken cancellationToken)
	{
		//todo: implement
		// var programmer = new HardwareProgrammer(deviceName, hardwareWriter);
		// await programmer.PrepareConfiguration();
		// await programmer.ConfigureDevice();
		throw new NotImplementedException();
	}
}
